import React from 'react';
import ReactDOM from 'react-dom';

// Create an element
const hello = <h1 style={{ color: 'blue' }}>Hello World</h1>;

// Render the element to the DOM
ReactDOM.render(hello, document.getElementById('root'));
